spring:
  application:
    name: Themeleaf
server:
  port: 8081
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false
  web:
   resources:
     static-locations: classpath:/static/
radio:
    options: Option A,Option B,Option C

initial:
  1:
    1:
      options: Option A,Option B,Option C


